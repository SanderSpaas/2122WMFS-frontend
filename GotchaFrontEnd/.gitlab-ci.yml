build site:
  image: node:latest
  stage: build
  script:
    - cd GotchaFrontEnd
    - npm install --progress=false

lint:
  stage: test
  script:
    - cd GotchaFrontEnd
    - npm run lint

unit test:
  stage: test
  script:
    - cd GotchaFrontEnd
    - npm install --progress=false
    - npm run test:unit 

